{
  "name": "spoochie",
  "version": "0.9.2",
  "private": true,
  "license": "MIT",
  "description": "A tunnel between two people's Claude Code sessions, with Slack for notifications and the thread",
  "type": "module",
  "bin": {
    "spoochie": "./src/cli.ts"
  },
  "scripts": {
    "test": "bun test",
    "daemon": "bun run src/daemon.ts",
    "spoochie": "bun run src/cli.ts"
  },
  "devDependencies": {
    "@types/node": "^26"
  },
  "dependencies": {
    "nostr-tools": "^2.25.2"
  }
}
